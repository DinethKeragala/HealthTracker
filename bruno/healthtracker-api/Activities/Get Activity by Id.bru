meta {
  name: Get Activity by Id
  type: http
  seq: 12
}

get {
  url: {{host}}/api/activities/:id
}

params:path {
  id: {{activityId}}
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("get activity", function () {
    expect(res.status).to.be.oneOf([200, 404]);
  });
}
