meta {
  name: Delete Goal
  type: http
  seq: 24
}

delete {
  url: {{host}}/api/goals/:id
}

params:path {
  id: {{goalId}}
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("delete goal", function () {
    expect(res.status).to.be.oneOf([200, 404]);
  });
}
