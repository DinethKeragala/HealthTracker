meta {
  name: List Goals
  type: http
  seq: 21
}

get {
  url: {{host}}/api/goals
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("list goals", function () {
    expect(res.status).to.equal(200);
    expect(res.body.items).to.be.an('array');
  });
}
