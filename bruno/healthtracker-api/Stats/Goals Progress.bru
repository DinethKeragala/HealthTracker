meta {
  name: Goals Progress
  type: http
  seq: 41
}

get {
  url: {{host}}/api/stats/goals-progress
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("goals progress", function () {
    expect(res.status).to.equal(200);
    expect(res.body.items).to.be.an('array');
  });
}
