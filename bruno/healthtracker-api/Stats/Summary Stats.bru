meta {
  name: Summary Stats
  type: http
  seq: 40
}

get {
  url: {{host}}/api/stats/summary
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("summary stats", function () {
    expect(res.status).to.equal(200);
    expect(res.body.totals).to.exist;
  });
}
